def compute_area(width, height, factor):
    area = (width + height) * factor // 2 - 5 * 2
    if area >= 100:
        #print("Area is large:", area)
    else:
        #print('Area is small:', area)

my_list = [1,2,3,4]

my_dict = {1,3,2}

class Rectangle:
    def __init__(self, width, height):
        self.width = width
        self.height = height
