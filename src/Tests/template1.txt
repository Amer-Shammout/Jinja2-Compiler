<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{ page_title|default("My Flask App") }}</title>
</head>
<body>
    <h1>{{ page_title }}</h1>

    <p>Hello, {{ user.name }}!</p>

    <a href={{ url }}>Link</a>

    <div class={{ class_name }}>Box</div>

    <p>Price: {{ "%.2f"|format(price) }}</p>

    <p>Computed: {{ (a + b) * 3 }}</p>
</body>
</html>