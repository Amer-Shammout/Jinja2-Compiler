x = 5

if x > 3:
    y = x + 1
    z = y * 2
elif x == 3:
    y = 0
else:
    y = -1

while y < 10 and not x == 0:
    y = y + 1

for i in x:
    y = y + i

def foo(a, b):
    if a < b:
        return a + b
    else:
        return -b

class A:
    pass
